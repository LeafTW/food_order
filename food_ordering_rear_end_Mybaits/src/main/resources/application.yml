spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Food_ordering
    username: root
    password: 20001024
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  mybatis:
    config-location: classpath:mybatis-config.xml # 可選：如果仍需使用mybatis-config.xml中的其他配置
  data:
    redis:
      host: localhost
      port: 6379

mybatis:
  mapper-locations: classpath:mapper/*.xml

